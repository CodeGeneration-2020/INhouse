import { makeStyles } from '@material-ui/core/styles';

export const pageSpinnerStyles = makeStyles(() => ({
  route_spinner: {
    alignItems: 'center',
    alignSelf: 'center',
    margin: '450px auto'
  }
}));