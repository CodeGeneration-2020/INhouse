import { makeStyles } from '@material-ui/core/styles';

export const NotFoundStyles = makeStyles(() => ({
  not_found: {
    display: 'flex',
    alignItems: 'center',
    height: '100vh',
    alignSelf: 'center',
    margin: '0 auto',
  }
}));
