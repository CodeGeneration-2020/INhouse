import { makeStyles } from '@material-ui/core/styles';

export const PreStyles = makeStyles(() => ({
  container: {
    maxHeight: '600px',
  },
  pre_section: {
    marginBottom: '50px',
  },
  pre_spinner: {
    margin: '50px',
  },
}));
